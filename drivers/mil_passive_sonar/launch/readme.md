# Example launch file

To run the passive sonar driver, personalize the parameters in the yaml config files in the config directory corresponding to your desired input mode.

Run the launch file in either serial or log mode. A sample log is included in the logs sub-directory.

> roslaunch mil_passive_sonar passive_sonar_example.launch input:=<serial|log>
